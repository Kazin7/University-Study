listy-style-type
text-decoration

justify-content
-space-between :첫번째와 마지막만 좌우정렬후 나머지 균등
-space-evenly: 전체 균등 정렬

flex-grow
flex-shrink 

order 속성
◦ flex item의 배치 순서 지정

미디어 쿼리를 적용하는 두가지 방법
1) @-규칙 - 스타일시트 내부에서 특정한 규칙을 표현하는 데 사용
 @media(<미디어 쿼리>){  //css코드 }

2) media 속성 -link 태그에 입력해서 해당 미디어 쿼리 조건에 맞는 장치에서 만CSS 파일을 불러올 때 사용
<link rel="stylesheet" href="파일이름" media="미디어쿼리">

생성자 함수와 클래스로 객체를 생성하는 경우 차이점

-생성자 함수는 new를 생략하면 일반 함수로 동작하고 new연산자를 사용하면 객체 생성
클래스는 반드시 new 연산자로 객체를 생성하며 생략할 수 없다

document.write일경우 ``이것을 사용해서 문장 출력

       프로토타입 함수 선언

        Car.prototype.addMileage = function(dist){
            this.dist += dist;
        }

1부터 9까지 랜덤값 가져오기
Math.floor(Math.random()*9) + 1;

textContent inerHtml

이벤트 등록 방법 3가지
1.인라인 이벤트 핸들러 방식
- HTML 요소의 이벤트 핸들러 속성에 이벤트 핸들러를 등록하는 방법

2.이벤트 핸들러 프로퍼티 방식
-요소 노드의 이벤트 속성으로 이벤트를 연결
-한번에 하나의 이벤트 리스너만 가질 수 있음

3.addEventListener 메소드 방식
- addEventListener 메소드를 이용하여 이벤트 핸들러 등록

window.onload = function () { //프로그램 실행시 바로 실행

DOM트리 노드 종류
1.문서 노드(document node) 
-트리의 최상위에 존재, DOM tree에 접근하기 위한 시작점(entry point)

2.요소 노드(element Node) 
- HTML 요소 표현 • 어트리뷰트, 텍스트 노드에 접근하려면 먼저 요소 노드를 찾아 접근 

3.어트리뷰트 노드(Attribute Node)
- HTML 요소의 어트리뷰트, 해당 요소 노드를 찾아 접근하면 참조, 수정 가능 

4.텍스트 노드(Text Node) 
- HTML 요소의 텍스트 표현, 요소 노드의 자식이며 자식 노드를 가질 수 없다

prompt 질문 + 값받기
alert 출력 alert($('h1').text())

eq(0)가 첫번째

jQuery 이용방법 두 가지
1) 파일을 다운로드하여 외부 자바스크립트 파일로 추가 
2) cdn 호스트 사용 

$(document).ready(function () {
            //jquery내용
 });
모든 자식
.children()

모든 부모
.parents(조건가능)

바로 다음 형제 선택
.next()

클래스 추가 
.addClass('클래스 이름');

속성 추가
.attr('width','300px');

첫번째가 아닌것 
.not(':first()')

for문 사용하는 법
$("input:checked").each(function () {
         str += $(this).val() + ' ';
});
val()이아니고 text()인경우는 select인경우

이벤트 처리방법
$('input[type=checkbox]').on('click', countChecked);
$('button').on("click", function () {  });


1초마다 함수 실행
id = setInterval(changeColor, 1000);

반복 종료
clearInterval(id)

내용 바꾸는 법
.text("내용");

select와 radio 에서 선택된 값 가져오기
a = $('#imglist option:selected').val();
b = "https://www." + $('input:radio[name=url]:checked').val();

값 입력된 것 ul에 붙여서 li로 저장& 삭제
<script>
        $(document).ready(function () {
            $('#add').on("click", function () {
                text = $('input').val();
                $('ul').append("<li>" + text + "</li>");
                $("li").css("list-style", 'none').css('border',"solid 1px gray").css('margin-bottom','10px');
            });
            $('#remove').on("click", function () {
                text = $('input').val();
                $("li").remove(`:contains(${text})`);
            });
        });
</script>


가장 뒤에있던 것을 앞으로 보내기
$('div').last().prependTo('body');

클릭하면 사라지게하는 애니메이션
$("div").on("click",function () {
        $(this).hide(2000, function () {
          $(this).remove();
        });
 });

클릭시 절반으로 감소
$("div").on("click",function () {
        $(this).animate({
           width:$(this).width()/2,
         },1000);
 });

$().hover(
  function(){
     //마우스가 올라가 있을경우
   },
   funtion(){
    //마우스가 올라가 있지 않을 경우
  }
);

$('.outer-menu-item').hover(function () {
                $(this).find('.inner-menu').show();
            }, function () {
                $(this).find('.inner-menu').hide();
});

mouseenter와 mouseover의 차이점은 mouseover는 자식 영역도 포함이지만 mouseenter는 자신의영역만 적용된다.

event.preventDefault(); //기본이벤트를 제거한다.

