모듈 정리
from gtts import gTTS
import playsound
from sklearn import datasets
from sklearn import svm
from sklearn.model_selection import train_test_split

digit=datasets.load_digits()
xtrain,xtest,ytrain,ytest=train_test_split(digit.data,digit.target,train_size=0.6)

s=svm.SVC(gamma=0.001)
s.fit(xtrain,ytrain)

res=s.predict(xtest)

conf=np.zeros((10,10))
for i in range(len(res)):
	conf[res[i][ytest[i]]+=1
print(conf)

no_correct=0
for i in range(10):
	no_correct+=conf[i][i]
accuracy = no_correct/len(res) *100

강한 인공지능
-다양한 지능의 복합체

약한 인공지능
-한가지 지능에 특화된 인공지능
ex)알파고,현재 인공지능 제품들

파이썬 사용방법
1.클라우드 방식
2.스탠드 얼론 방식

from - import -로 사용

dir() -> 클래스의 멤버 변수와 멤버 함수 확인
type() -> 어떤 클래스인지 확인

기계학습: 사람이 아니라 기계가 규칙을 만드는 것

인공지능 > 기계학습 > 신경망 > 딥러닝

기계학습의 용어
1.데이터셋
2.특징 벡터
3.클래스

원핫코드
한요소만 1인 이진열
(1,0 0) (0,0,1) (0,1,0) 

훈련집합 : 기계 학습 모델을 학습하는데 쓰는 데이터(모델링)

테스트집합 : 학습을 마친 모델의 성능을 측정하는데 사용하는 데이터(예측)

데이터 편향

~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
규칙기반 -> 분류하는 규칙자체를 사람이 다함

기계학습 -> 특징벡터 추출과 레이블을 사람이함 ,규칙은 기계가함

딥러닝 -> 레이블만 사람이함, 특정 벡터를 학습이 자동으로 알아냄
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

두점의 거리
루트 (x1-x2)^2

기계-++학습 과정
데이터 수집 -> 특징 추출 -> 모델링 -> 예측

수치형 특징
-거리 개념이 있음
범주형 특징

순서형
거리 개념이 있음

이름형
거리 개념이 없음

	긍정 부정
긍정	TP    FP
부정	FN   TN

정확률
대각선의 값 / 전체 값

특이도
실제 부정값 / 전체 부정값

민감도
실제 긍정값 / 전체 긍정값



정밀도
실제 긍정값 / 긍정 예측값 

재현율
실제 긍정값 / 전체 긍정값


오비피팅을 회피한다
아웃라이어를 잘라낸다

C라는 하이퍼 매개변수
클수록 훈련 집합에 대한 정확률은 높지만 일반화가 떨어진다
작을수록 훈련 집합에 대한 정확률은 낮지만 일반화가 높아진다

O = r(wx^T + w0)

기울기에 -를 붙여서 최소의 손실함수를 찾는다

경사 하강법은 미분을 이용하여 최적해를 찾는 기법

학습률p를 사용하여 조금씩 이동

퍼셉트론의 학습 규칙
-w = w+ p 시그마 yx

손실함수
분류
물체 검출
물체 분할

배치모드
-틀린샘플들을 모은뒤 한번에 갱신

패턴모드
-샘플마다 매개변수 갱신

기계학습의 디자인 패턴
데이터 읽기 - 모델 객체 생성 - 모델 학습 - 예측 - 성능 평가

퍼셉트론은 선형이라는 한계가있다

다층 퍼셉트론
입력층 - 은닉층 - 출력층

sj = uj^1 * x^T

계산 연습해봐
코드삽입문제
용어설명
역사나옴


계단함수
소프트맥스 함수 ->확률


오류 역전파 학습 알고리즘
출력층에서 시작하여 역방향으로 오류를 전파한다는 뜻

전방 계산 - 오차 계산 - 오류 역전파